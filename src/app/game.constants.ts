

import {} from './game.config';
import { environment } from './../environments/environment';

export const GAMEPOSTURL = Object.freeze({

    ALPHABETIC:{
        getanatomy: "assets/appData.json"
    }
})